LegalMind


To run the project

1) Frontend :
    -cd client
    -npm install
    -npm run dev

2) Backend :
    -cd server
    -python -m venv .venv{Any name you want}[Run this command only once]
    -.venv/scripts/activate 
    -pip install -r requirements.txt
    -python app.py
